[package]
name = "bevy_entitiles"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/443eb9/bevy_entitiles"
exclude = [
    "test/*"
]
description = "A tilemap library for bevy."

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.11.3", default-features = false, features = ["dynamic_linking", "trace"] }

[dev-dependencies]
tracing-tracy = "0.10.4"

[dev-dependencies.bevy]
version = "0.11.3"
default-features = false
features = [
    "bevy_core_pipeline",
    "bevy_render",
    "bevy_asset",
    "bevy_gizmos",
    "bevy_ui",
    "png",
    "bevy_winit",
    "bevy_text",
    "bevy_sprite",
    "default_font",
    "dynamic_linking",
    "trace_tracy",
    "multi-threaded"
]

[features]
default = []
debug = []

[[example]]
name = "test"
path = "examples/test.rs"
required-features=["debug"]
